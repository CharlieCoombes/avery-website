# Public environment variables (exposed to client-side code)
PUBLIC_SITE_URL=https://averyapp.ai

# Analytics (add when needed)
# PUBLIC_GA_MEASUREMENT_ID=
# PUBLIC_PLAUSIBLE_DOMAIN=

# API endpoints (add when needed)
# API_BASE_URL=
# STRIPE_PUBLIC_KEY=

# Internal configuration
# NODE_ENV=production
